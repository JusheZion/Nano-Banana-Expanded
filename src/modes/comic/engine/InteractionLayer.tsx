import React from 'react';

export const InteractionLayer: React.FC = () => {
    return (
        <div className="interaction-layer absolute inset-0 pointer-events-none">
            {/* Handles selection, resizing, and snapping */}
        </div>
    );
};
